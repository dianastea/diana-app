import { ElementIconsImage } from ".";

export default {
  title: "Components/ElementIconsImage",
  component: ElementIconsImage,
};

export const Default = {
  args: {
    darkMode: true,
    shape: "round",
    className: {},
    visible: true,
  },
};
